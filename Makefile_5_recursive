$ cat v1
LIBDIRS = bar foo
MAINDIR = main1 main2

all : $(LIBDIRS) $(MAINDIR)
foo : bar
main1 : foo
main2 : foo

$(MAINDIR) : $(LIBDIRS)
$(LIBDIRS) :
	$(MAKE) -C $@
	cp $@/$@ $@/lib$@.so
$(MAINDIR) :
	$(MAKE) -C $@
clean :
	for dir in $(LIBDIRS) $(MAINDIR); do \
		$(MAKE) -C $$dir $@; \
	done
.PHONY : $(LIBDIRS) $(MAINDIR) all clean
$

$ cat v2
SUBDIRS = bar foo main
subdirs : $(SUBDIRS)
$(SUBDIRS) :
	make -C $@
	cp $@/$@ $@/lib$@.so
clean :
	for dir in $(SUBDIRS); do \
		make -C $$dir $@; \
	done
.PHONY : $(SUBDIRS) subdirs clean

main :
	make -C $@

main : foo
foo : bar
$
